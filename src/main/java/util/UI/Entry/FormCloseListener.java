package util.UI.Entry;

public interface FormCloseListener {

    /**
     * This method is called, when the window is closed.
     *
     * @param form the form object is given.
     */
    void onClose(Form form);

}
